name: build Dockers
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 1"
  push:
    branches:
      - main
    paths:
      - dockers/**
jobs:
  push:
    name: "gta:${{ matrix.game }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        game:
          - "3proxy"
          
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v2
        with:
          buildkitd-flags: --debug
      - uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v4
        with:
          context: ./proxy/${{ matrix.game }}
          file: ./proxy/${{ matrix.game }}/dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/vncscode/dockers:${{ matrix.game }}
